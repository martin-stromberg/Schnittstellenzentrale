<div class="request-query-params-panel">
    <table class="table table-sm">
        <thead>
            <tr>
                <th>Name</th>
                <th>Wert</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var param in QueryParameters.OrderByDescending(p => p.IsPathParameter))
            {
                <tr @key="param">
                    <td>
                        <input class="form-control form-control-sm" value="@param.Key" @onchange="(e) => OnFieldChanged(param, e, isKey: true)" />
                    </td>
                    <td>
                        <input class="form-control form-control-sm" value="@param.Value" @onchange="(e) => OnFieldChanged(param, e, isKey: false)" />
                    </td>
                    <td>
                        @if (!param.IsPathParameter)
                        {
                            <button type="button" class="btn btn-outline-danger btn-sm" @onclick="() => RemoveParam(param)">&#x2715;</button>
                        }
                    </td>
                </tr>
            }
        </tbody>
    </table>
    <button type="button" class="btn btn-outline-secondary btn-sm" @onclick="AddParam">+ Parameter</button>
</div>

@code {
    [Parameter] public List<QueryParamEntry> QueryParameters { get; set; } = [];
    [Parameter] public EventCallback OnChanged { get; set; }

    public class QueryParamEntry
    {
        public string Key { get; set; } = string.Empty;
        public string Value { get; set; } = string.Empty;
        public bool IsPathParameter { get; set; }
    }

    private async Task OnFieldChanged(QueryParamEntry entry, ChangeEventArgs e, bool isKey)
    {
        var value = e.Value?.ToString() ?? string.Empty;
        if (isKey)
            entry.Key = value;
        else
            entry.Value = value;
        await OnChanged.InvokeAsync();
    }

    private async Task AddParam()
    {
        QueryParameters.Add(new QueryParamEntry());
        await OnChanged.InvokeAsync();
    }

    private async Task RemoveParam(QueryParamEntry entry)
    {
        QueryParameters.Remove(entry);
        await OnChanged.InvokeAsync();
    }
}
